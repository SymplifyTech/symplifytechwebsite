import { defineConfig } from 'vite';

export default defineConfig({
    // If you are using a custom domain (e.g., www.symplifytech.com), change this to '/'
    // If you are using the default GitHub Pages URL (srijanrana77.github.io/symplifytechwebsite), leave it as '/symplifytechwebsite/'
    base: '/',
});
